/**
 * Base de l'API Laravel (sans slash final).
 * Ex. Laragon : http://localhost/sifec/public/api/v1
 * Ajuster selon votre déploiement ou utiliser un chemin relatif si même origine.
 */
window.PORTAIL_CONFIG = window.PORTAIL_CONFIG || {
  apiBase: '../public/api/v1',
};
