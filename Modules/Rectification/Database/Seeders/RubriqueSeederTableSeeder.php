<?php

namespace Modules\Rectification\Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;
use Illuminate\Database\Eloquent\Model;

class RubriqueSeederTableSeeder extends Seeder
{
    //     DB::statement('set foreign_key_checks = 0');
    //     DB::statement("truncate tr_rubrique");

    //     $donnees = ["nom","MARIAGE","DIVORCE","DECES"];
    //     $data = [];

    //     for($i = 0; $i < count($donnees); $i++){
    //         $count = $i +1;
    //         $strCode = "TAC_".str_pad($count,4,"0",STR_PAD_LEFT);
    //         $data[] = ["code_type_acte"=>$strCode,"lib_type_acte"=>$donnees[$i]];
    //     }

    //    foreach($data as $d){
    //         TypeActe::create($d);
    //    }
}
